<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body ng-app="awesomeApp">

  <% include ../partials/nav.ejs %>

		<div class="container" ng-controller="QuizDescriptorCtrl as quizDescriptors">

			<div class="col-sm-6 col-sm-offset-2">

	    		<h1><span class="fa"></span> Quizzes </h1>

	    		<hr>

				<div>

				    <ul class="quizzes">
					  <li ng-repeat="quiz in quizDescriptors.quizzes">
					    <span><b>{{quiz.id}}</b> </span>
					    <span style="padding-right:10px;"> </span>
				        <a href="/quizdescriptor/{{ quiz.id }}">{{ quiz.descriptor.title }}</a>
					  </li>
					</ul>

				</div>

			</div>
			
		</div>


</body>
</html>
